# Pinned to versions used in local .venv312 (Python 3.12).
# torch 2.11 ships CUDA 12.x wheels by default; on HPC GPU nodes that's fine.
# If the cluster CUDA driver is older, pin to a matching torch+cu118 wheel instead.
torch==2.11.0
gensim==4.4.0
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8
tqdm==4.67.3
